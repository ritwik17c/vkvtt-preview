/* VKV Nalbari Timetable — Cloud v66.6 Dashboard Add-on */
(() => {
  const VERSION='66.0-leave-fix-1';
  const style=document.createElement('style');style.textContent=`.tile,button:not(:disabled),.btn,a[href],[role="button"],summary,[onclick]{cursor:pointer!important}button:disabled{cursor:not-allowed!important}.v64QuickLeaveFrame{width:100%;min-height:900px;border:1px solid #cbdce5;border-radius:16px;background:#f7fbf9}@media(max-width:700px){.v64QuickLeaveFrame{min-height:1180px}}`;document.head.appendChild(style);
  function removeLegacyNonTeachingTile(tiles){[...tiles.querySelectorAll('.tile')].forEach(tile=>{if(['v663StaffManagementTile','v666NoticeBoardTile'].includes(tile.id))return;const text=(tile.textContent||'').replace(/\s+/g,' ').trim().toLowerCase(),click=String(tile.getAttribute('onclick')||tile.dataset?.href||'').toLowerCase(),href=String(tile.querySelector('a[href]')?.getAttribute('href')||'').toLowerCase();if((text.includes('non-teaching staff')||text.includes('non teaching staff')||click.includes('nonteaching')||click.includes('non-teaching')||href.includes('nonteaching')||href.includes('non-teaching'))&&!text.includes('staff management'))tile.remove()})}
  function install(){
    const tiles=document.querySelector('#dashboardHome .tiles');if(!tiles)return;removeLegacyNonTeachingTile(tiles);
    if(!document.getElementById('v663StaffManagementTile')){const staff=document.createElement('div');staff.className='tile';staff.id='v663StaffManagementTile';staff.style.cssText='background:linear-gradient(145deg,#eef8ff,#edf8f2);border-color:#91bfd0';staff.innerHTML='<b>👥 Staff Management</b><span>Personnel, Employee Codes, qualifications, service details, inactive archive and Excel import/export.</span>';const teachers=[...tiles.querySelectorAll('.tile')].find(x=>/Teachers\s*&\s*Workload/i.test(x.textContent||''));if(teachers)teachers.insertAdjacentElement('beforebegin',staff);else tiles.appendChild(staff);staff.onclick=()=>location.href='admin-staff-management-v7.html?v=66.6'}
    if(!document.getElementById('v666NoticeBoardTile')){const n=document.createElement('div');n.className='tile';n.id='v666NoticeBoardTile';n.style.cssText='background:linear-gradient(145deg,#fff8df,#eef8ff);border-color:#d8bd77';n.innerHTML='<b>📌 Staff Notice Board</b><span>Create, pin, expire, edit and remove notices visible to all teaching and non-teaching staff.</span>';const staff=document.getElementById('v663StaffManagementTile');if(staff)staff.insertAdjacentElement('afterend',n);else tiles.appendChild(n);n.onclick=()=>location.href='admin-staff-notice-board.html?v=66.6'}
    if(!document.getElementById('v64QuickLeaveTile')){const quick=document.createElement('div');quick.className='tile';quick.id='v64QuickLeaveTile';quick.style.cssText='background:#eef8ff;border-color:#a9cfe2';quick.innerHTML='<b>➕ Quick Add Leave</b><span>Add an individual Date Row or Date-Range Row directly from the Admin Dashboard.</span>';const leaveEditor=document.getElementById('openLeaveEditor');if(leaveEditor)leaveEditor.insertAdjacentElement('afterend',quick);else tiles.appendChild(quick);const panel=document.createElement('section');panel.id='v64QuickLeavePanel';panel.className='card panel';panel.innerHTML=`<div class="sectionTop"><div><div class="breadcrumb">Admin Dashboard → Quick Add Leave</div><h2>➕ Quick Add Leave</h2></div><button type="button" data-v64-back>← Admin Dashboard</button></div><div class="help">This is the same Leave Editor and the same Firestore leave data. Each click adds one Date Row or one Date-Range Row.</div><iframe class="v64QuickLeaveFrame" title="Quick Add Leave" src="admin-leave-editor.html?v=${VERSION}&quick=1"></iframe>`;document.getElementById('app')?.appendChild(panel);quick.onclick=()=>{document.querySelectorAll('.panel').forEach(p=>p.classList.remove('active'));const home=document.getElementById('dashboardHome');if(home)home.style.display='none';panel.classList.add('active');panel.scrollIntoView({behavior:'smooth',block:'start'})};panel.querySelector('[data-v64-back]')?.addEventListener('click',()=>{panel.classList.remove('active');const home=document.getElementById('dashboardHome');if(home)home.style.display='block';window.scrollTo({top:0,behavior:'smooth'})})}
    removeLegacyNonTeachingTile(tiles);const sub=document.querySelector('header .subtitle');if(sub)sub.textContent=sub.textContent.replace(/Cloud v\d+(?:\.\d+)?/,'Cloud v66.6');
  }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();setTimeout(install,700);setTimeout(install,1800);
})();